import { SearchProductsDirective } from './search-products.directive';

describe('SearchProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchProductsDirective();
    expect(directive).toBeTruthy();
  });
});
